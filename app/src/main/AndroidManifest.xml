<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml  — versão atualizada
    Única alteração: adição da declaração de RegisterMedicoActivity.
    Todo o restante foi preservado exatamente como estava no original.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicoApplication"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.auth_e_cadastro.LoginActivity"            android:exported="false" />
        <activity android:name=".activities.auth_e_cadastro.RegisterActivity"         android:exported="false" />

        <!-- ✅ NOVO: Cadastro de Médico -->
        <activity android:name=".RegisterMedicoActivity"   android:exported="false" />

        <activity android:name=".activities.auth_e_cadastro.ForgotPasswordActivity"   android:exported="false" />
        <activity android:name=".activities.paciente.HomePacienteActivity"     android:exported="false" />
        <activity android:name=".activities.medico.HomeMedicoActivity"       android:exported="false" />
        <activity android:name=".activities.auth_e_cadastro.ResetPasswordActivity"    android:exported="false" />
        <activity android:name=".Cadastro"                 android:exported="false" />

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>